<Window x:Class="HairCare.FilterWindow"

        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

        xmlns:local="clr-namespace:HairCare"

        mc:Ignorable="d"

        WindowStartupLocation="CenterOwner"

        Title="Пошук" Height="420" Width="450">

    <Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Column="0"  Grid.Row="0">

            <StackPanel >

                <Label Name="SelectViewState" Content="Пошук записів за батьківськими(базовими) характеристиками" Background="WhiteSmoke" BorderBrush="Gray" BorderThickness="1"/>



                <ItemsControl Name="SelectProductView">

                    <ItemsControl.ItemTemplate>

                        <DataTemplate>

                            <DockPanel>

                                <TextBlock Text="{Binding Path = StatName}" Margin="5" TextAlignment="Left"/>

                                <TextBox TextWrapping="Wrap" Name="SelectProductViewTextBox" Margin="5" Text="{Binding Path = StatValue, UpdateSourceTrigger = Explicit}"/>

                            </DockPanel>

                        </DataTemplate>

                    </ItemsControl.ItemTemplate>

                </ItemsControl>

                

                <Button Content="Застосувати" Click="OnConfirmFilterClicked" Margin="5"/>

                <Button Content="Відмінити" Click="OnCancelFilterClicked" Margin="5"/>

            </StackPanel>

        </ScrollViewer>

    </Grid>

</Window>

