<Window x:Class="HairCare.CreateProductWindow"

        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

        mc:Ignorable="d"

        WindowStartupLocation="CenterOwner"

        Title="Створення запису" Height="Auto" Width="450">

    <Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Column="0"  Grid.Row="0">

            <StackPanel >

                <Label Name="SelectViewState" Content="Створення запису" Background="WhiteSmoke" BorderBrush="Gray" BorderThickness="1"/>



                <ItemsControl Name="SelectProductView">

                    <ItemsControl.ItemTemplate>

                        <DataTemplate>

                            <DockPanel>

                                <TextBlock Text="{Binding Path = StatName}" Margin="5" TextAlignment="Left"/>

                                <TextBox TextWrapping="Wrap" Name="SelectProductViewTextBox" Margin="5" Text="{Binding Path = StatValue, UpdateSourceTrigger = Explicit}"/>

                            </DockPanel>

                        </DataTemplate>

                    </ItemsControl.ItemTemplate>

                </ItemsControl>

                

                <Button Content="Створити" Click="OnConfirmClicked" Margin="5"/>

                <Button Content="Відмінити" Click="OnCancelClicked" Margin="5"/>

            </StackPanel>

        </ScrollViewer>

    </Grid>

</Window>

